#include <stdio.h>

int main(){
    /*
    a) Function can only return one value at a time.
    b) Changes to parameters in function don't change the values in

    calling function.asm
    Because a copy of argument is passed to the function.
    */
    return 0;
}